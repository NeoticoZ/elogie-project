import styled from "styled-components";

export const ElogieLogoContainer = styled.div`
  a {
    text-decoration: none;
    font-weight: bold;
    color: var(--bs-gray-900);

    span {
      color: var(--bs-blue);
    }
  }
`;
